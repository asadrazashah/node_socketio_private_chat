const {
  sendEmail,
  sendForgotPasswordByEmail,
  sendResetEmail,
} = require("./email");
const helpers = {
  sendEmail,
  sendForgotPasswordByEmail,
  sendResetEmail,
};
module.exports = {
  helpers,
};
